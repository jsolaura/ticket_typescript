export type passwordInputType = {
    currentPassword: string, newPassword: string, checkPassword: string
}
export type passwordChangeType = {
    type: string, visible: boolean
}
export type listType = {
    order: any,
    used: boolean,
    status: number
}